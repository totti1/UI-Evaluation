import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  image: {
    // flex: 1,
    // position: 'absolute',
    marginTop: 20,
    marginLeft: 10,
    height: 200,
    width: 295,
    zIndex: 1
  }
})
